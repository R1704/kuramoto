export function updateManifoldVisibility() {
    // Manifold-specific visibility is currently handled in updateDisplay.
}

export function updatePatternOptions() {
    // Pattern options are currently static in UIManager bindings.
}
